# Gemini CLI Configuration

## Git Operations Policy

The user prefers to handle all `git` operations manually. The agent **must not** execute `git add`, `git commit`, `git push`. Read only `git` operations are permitted (`git status`, etc).
